<UserControl x:Class="CustomDeliveryNoteDemo.NoteSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomDeliveryNoteDemo"
             mc:Ignorable="d" 
             IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
             d:DesignHeight="655" d:DesignWidth="1015" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="1" Grid.ColumnSpan="2"
               Grid.Row="0"
               Content="Search:"
               HorizontalAlignment="Left"
               HorizontalContentAlignment="Left"
               VerticalContentAlignment="Center"
               VerticalAlignment="Center"
               Padding="0"
               FontSize="{Binding LabelFontSize}"/>

        <TextBox Grid.Column="3" Grid.ColumnSpan="2"
                 Grid.Row="0"
                 Template="{StaticResource DeliveryNoteTextBoxTemplate}"
                 HorizontalAlignment="Stretch"
                 HorizontalContentAlignment="Left"
                 TextWrapping="Wrap"
                 VerticalContentAlignment="Center"
                 VerticalAlignment="Center"
                 FontSize="{Binding LabelFontSize}"/>

        <Button Grid.Row="3"
                Grid.Column="0" Grid.ColumnSpan="2"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                Height="28"
                Width="28"
                IsEnabled="{Binding IsBusy}"
                Style="{StaticResource DeliveryNoteButtonTemplate}">
            <Image Source = "Pictures/EyeIcon.png"                   
                   Width="24"
                   Height="24">
            </Image>
        </Button>

        <Button Grid.Row="4"
                Grid.Column="0" Grid.ColumnSpan="2"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                Height="28"
                Width="28"
                IsEnabled="{Binding IsBusy}"
                Style="{StaticResource DeliveryNoteButtonTemplate}">
            <Image Source = "Pictures/LikeIcon.png"
                   Width="24"
                   Height="24">
            </Image>
        </Button>

        <Button Grid.Row="5"
                Grid.Column="0" Grid.ColumnSpan="2"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                Height="28"
                Width="28"
                IsEnabled="{Binding IsBusy}"
                Style="{StaticResource DeliveryNoteButtonTemplate}">
            <Image Source = "Pictures/InfirmIcon.png"
                   Width="24"
                   Height="24">
            </Image>
        </Button>

        <ListView Grid.Row="2" Grid.RowSpan="5"
                  Grid.Column="2" Grid.ColumnSpan="6"
                  DataContext="{Binding NoteLineVMList}"          
                  Template="{StaticResource SearchNoteLineListViewTemplate}">
        </ListView>
    </Grid>
</UserControl>
